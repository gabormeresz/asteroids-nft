import { jura } from "@/fonts";

const CollectionPage = () => {
  return (
    <div
      className={`text-center text-white text-xl font-light ${jura.variable} font-mono`}
    >
      Coming Soon...
    </div>
  );
};

export default CollectionPage;
